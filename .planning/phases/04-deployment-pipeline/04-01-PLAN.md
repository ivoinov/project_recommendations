---
phase: 04-deployment-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - celery_app/worker_health.py
  - Dockerfile_celery
autonomous: true

must_haves:
  truths:
    - "Worker container exposes an HTTP health endpoint for deploy verification"
    - "Deployed worker can be checked without auth via /health"
  artifacts:
    - path: "celery_app/worker_health.py"
      provides: "FastAPI health/readiness endpoints for worker"
      exports: ["app"]
    - path: "Dockerfile_celery"
      provides: "Runs Celery worker alongside health server"
      contains: "uvicorn celery_app.worker_health:app"
  key_links:
    - from: "Dockerfile_celery"
      to: "celery_app/worker_health.py"
      via: "uvicorn command"
      pattern: "uvicorn\s+celery_app\.worker_health:app"
---

<objective>
Add a lightweight HTTP health endpoint to the Celery worker container so CI/CD can verify the worker is running.

Purpose: Enable automated deploy verification for the worker in the hosting environment.
Output: Worker health app and updated worker container entrypoint.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deployment-pipeline/04-CONTEXT.md
@.planning/phases/04-deployment-pipeline/04-RESEARCH.md
@Dockerfile_celery
@celery_app/celery_worker.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create worker health FastAPI app</name>
  <files>celery_app/worker_health.py</files>
  <action>
    Create a minimal FastAPI app with unauthenticated `/health` and `/readiness` endpoints.
    - `/health` returns `{status: "healthy", service: "recommendation-worker"}`.
    - `/readiness` checks Redis broker connectivity using `CELERY_BROKER_URL` and returns ready/not_ready.
    - Keep dependencies limited to existing stack (fastapi, redis).
  </action>
  <verify>python -c "from celery_app.worker_health import app; print([r.path for r in app.routes])"</verify>
  <done>Worker health app module imports and exposes `/health` and `/readiness` routes.</done>
</task>

<task type="auto">
  <name>Task 2: Run health server alongside Celery worker</name>
  <files>Dockerfile_celery</files>
  <action>
    Update the worker container command to start a Uvicorn server for the health app and the Celery worker in the same container.
    - Use `PORT` env var with default 8080 for the health server.
    - Keep existing Celery command options unchanged.
    - Ensure the health server binds to `0.0.0.0`.
  </action>
  <verify>docker build -f Dockerfile_celery -t recommendation-worker:health .</verify>
  <done>Worker container starts a health server on port 8080 and still runs Celery worker.</done>
</task>

</tasks>

<verification>
- Worker health server responds with 200 on `/health` when container is running.
</verification>

<success_criteria>
- Worker image includes an HTTP health endpoint usable by CI/CD verification.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deployment-pipeline/04-01-SUMMARY.md`
</output>
