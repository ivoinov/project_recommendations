---
phase: 03-reliable-batch-publishing-and-candidate-store
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - app/services/precomputed_recommendation_service.py
  - app/routers/baseline_recommendations.py
  - app/routers/publish_status.py
  - app/routers/publish_schemas.py
  - main.py
  - tests/test_baseline_recommendations.py
  - tests/test_publish_status.py
autonomous: true

must_haves:
  truths:
    - "Daily batch refresh runs and records a success/failure status per shop."
    - "PDP/cart endpoints serve recommendations from precomputed candidates with Redis cache enabled."
    - "Publish state tracks current/previous version and supports rollback to previous version."
  artifacts:
    - path: "app/services/precomputed_recommendation_service.py"
      provides: "Read path from candidate store with cache-aside behavior."
    - path: "app/routers/baseline_recommendations.py"
      provides: "PDP/cart endpoints wired to precomputed candidates."
    - path: "app/routers/publish_status.py"
      provides: "Publish status + rollback endpoints."
  key_links:
    - from: "app/routers/baseline_recommendations.py"
      to: "app/services/precomputed_recommendation_service.py"
      via: "serve-time candidate fetch"
      pattern: "get_precomputed_"
    - from: "app/services/precomputed_recommendation_service.py"
      to: "app/cache/recommendation_cache.py"
      via: "cache-aside read"
      pattern: "get_cached_candidates|set_cached_candidates"
    - from: "app/routers/publish_status.py"
      to: "app/repositories/recommendation_publish_repository.py"
      via: "status lookup and rollback"
      pattern: "get_state|update_publish_state"
---

<objective>
Serve recommendations from the precomputed candidate store with cache-aside behavior and expose publish status/rollback APIs.

Purpose: Fulfill SRV-02 and OPS-02 by serving cached, versioned candidates and enabling rollback visibility.
Output: Updated recommendation endpoints, publish status router, and tests for the new behavior.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-reliable-batch-publishing-and-candidate-store/03-CONTEXT.md
@app/routers/baseline_recommendations.py
@app/services/baseline_recommendation_service.py
@app/cache/recommendation_cache.py
@app/repositories/recommendation_candidate_repository.py
@app/repositories/recommendation_publish_repository.py
@tests/test_baseline_recommendations.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add precomputed recommendation read service and wire PDP/cart endpoints</name>
  <files>
app/services/precomputed_recommendation_service.py
app/routers/baseline_recommendations.py
  </files>
  <action>
Create app/services/precomputed_recommendation_service.py with functions that:
- Resolve current version from RecommendationPublishRepository for a placement.
- Fetch candidate lists by candidate_type (co_purchase, content_based, merged) for each seed SKU and version.
- Use Redis cache-aside: build cache keys with shop_id/placement/seed/version; on miss load from DB, then cache.
- Merge multi-seed cart candidates by score desc with dedupe; fall back to merged list if per-type lists are empty.
- Apply business rules at serve time by calling filter_candidates from baseline_recommendation_service.
- Respect caps: PDP 20, Cart 50 (use request limit for cart if lower).

Update baseline_recommendations router to call the precomputed service for both PDP and cart, preserving the response shape (co_purchase/content_based/recommendations).
If no publish state or candidates exist, return empty lists with correct placement/seed_skus.
  </action>
  <verify>python -c "from app.services.precomputed_recommendation_service import get_pdp_recommendations"</verify>
  <done>Endpoints return precomputed, cached candidates and still apply in-stock/exclusion rules at serve time.</done>
</task>

<task type="auto">
  <name>Task 2: Add publish status + rollback endpoints with tests</name>
  <files>
app/routers/publish_status.py
app/routers/publish_schemas.py
main.py
tests/test_baseline_recommendations.py
tests/test_publish_status.py
  </files>
  <action>
Add a new router under /v1/shops/{shop_id}/publishes with auth + shop-scoped DB:
- GET /status returns per-placement current_version, previous_version, and latest run status/date/message.
- POST /rollback accepts placement and resets current_version to previous_version (no-op if missing), then invalidates cache keys for that placement.

Create publish schemas for status response and rollback request/response.
Register the router in main.py.

Update tests:
- Adjust tests/test_baseline_recommendations.py to seed candidate store + publish state before calling endpoints, asserting results come from precomputed lists and rules still apply.
- Add tests/test_publish_status.py to cover status response and rollback behavior for a shop schema.
  </action>
  <verify>python -m pytest tests/test_baseline_recommendations.py tests/test_publish_status.py</verify>
  <done>Status and rollback endpoints work with shop-scoped schemas, and recommendation tests validate precomputed serving.</done>
</task>

</tasks>

<verification>
- PDP/cart endpoints read from candidate store and cache keys include shop_id + version.
- Status endpoint reports current/previous versions and latest run state.
- Rollback updates publish state and invalidates cache for the placement.
</verification>

<success_criteria>
- SRV-02: recommendations are served from precomputed candidates with cache-aside reads.
- OPS-02: per-shop publish status and rollback to previous version are available.
</success_criteria>

<output>
After completion, create `.planning/phases/03-reliable-batch-publishing-and-candidate-store/03-03-SUMMARY.md`
</output>
