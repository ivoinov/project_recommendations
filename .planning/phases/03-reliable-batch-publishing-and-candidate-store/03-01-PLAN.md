---
phase: 03-reliable-batch-publishing-and-candidate-store
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/recommendation_candidate.py
  - app/models/recommendation_publish_state.py
  - app/models/recommendation_publish_run.py
  - app/models/__init__.py
  - app/repositories/recommendation_candidate_repository.py
  - app/repositories/recommendation_publish_repository.py
  - app/repositories/__init__.py
  - app/cache/recommendation_cache.py
  - app/cache/__init__.py
  - .env.sample
autonomous: true

must_haves:
  truths:
    - "Daily batch refresh runs and records a success/failure status per shop."
    - "PDP/cart endpoints serve recommendations from precomputed candidates with Redis cache enabled."
    - "Publish state tracks current/previous version and supports rollback to previous version."
  artifacts:
    - path: "app/models/recommendation_candidate.py"
      provides: "Versioned candidate rows per seed/placement."
    - path: "app/models/recommendation_publish_state.py"
      provides: "Current/previous version pointers per placement."
    - path: "app/models/recommendation_publish_run.py"
      provides: "Per-shop publish run status and data counts."
    - path: "app/cache/recommendation_cache.py"
      provides: "Redis cache access for candidate lists."
  key_links:
    - from: "app/repositories/recommendation_publish_repository.py"
      to: "app/models/recommendation_publish_state.py"
      via: "publish state upsert"
      pattern: "update_publish_state"
    - from: "app/repositories/recommendation_candidate_repository.py"
      to: "app/models/recommendation_candidate.py"
      via: "bulk insert and version delete"
      pattern: "create_candidates"
    - from: "app/cache/recommendation_cache.py"
      to: "redis client"
      via: "cache get/set/del"
      pattern: "get_cached_candidates|set_cached_candidates|delete_cache_keys"
---

<objective>
Define the candidate store schema, publish metadata, and cache utility primitives.

Purpose: Establish the data layer required for versioned publishes and cached serving.
Output: ORM models, repositories, and Redis cache helpers that other plans will use.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-reliable-batch-publishing-and-candidate-store/03-CONTEXT.md
@.planning/phases/03-reliable-batch-publishing-and-candidate-store/03-RESEARCH.md
@app/models/base.py
@app/repositories/__init__.py
@.env.sample
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add candidate + publish metadata ORM models</name>
  <files>
app/models/recommendation_candidate.py
app/models/recommendation_publish_state.py
app/models/recommendation_publish_run.py
app/models/__init__.py
  </files>
  <action>
Create three SQLAlchemy models using Base with snake_case table names:
- recommendation_candidates: id (pk), seed_sku, placement, candidate_type ("co_purchase"/"content_based"/"merged"), candidate_sku, score, version, created_at (UTC now).
- recommendation_publish_state: id (pk), placement, current_version, previous_version, updated_at (UTC now).
- recommendation_publish_runs: id (pk), run_date (date), status ("success"/"failed"/"skipped"), product_count, order_count, message, created_at.

Ensure placement is indexed on state and candidates; version indexed on candidates; seed_sku and candidate_type indexed for read path.
Export the new models from app/models/__init__.py to keep repository imports consistent.
  </action>
  <verify>python -c "from app.models import RecommendationCandidate, RecommendationPublishState, RecommendationPublishRun"</verify>
  <done>New ORM models exist and are importable from app.models with correct fields and indexes.</done>
</task>

<task type="auto">
  <name>Task 2: Add repositories and Redis cache utilities</name>
  <files>
app/repositories/recommendation_candidate_repository.py
app/repositories/recommendation_publish_repository.py
app/repositories/__init__.py
app/cache/recommendation_cache.py
app/cache/__init__.py
.env.sample
  </files>
  <action>
Create repositories following existing patterns:
- RecommendationCandidateRepository: create_candidates(seed_sku, placement, candidate_type, version, candidates_with_scores), get_candidates(seed_sku, placement, candidate_type, version, limit), delete_versions_older_than(placement, keep_versions=2).
- RecommendationPublishRepository: get_state(placement), update_publish_state(placement, current_version, previous_version), create_run(run_date, status, product_count, order_count, message), get_latest_run(), get_run_for_date(run_date).

Add app/cache/__init__.py and app/cache/recommendation_cache.py with Redis helpers using REDIS_CACHE_URL env (fallback to CELERY_BROKER_URL):
- get_redis_client() with decode_responses=True
- build_cache_key(shop_id, placement, seed_sku, version)
- get_cached_candidates(cache_key) and set_cached_candidates(cache_key, payload)
- delete_cache_keys(cache_keys)

Update .env.sample to include REDIS_CACHE_URL placeholder.
  </action>
  <verify>python -c "from app.repositories import RecommendationCandidateRepository, RecommendationPublishRepository; from app.cache import recommendation_cache"</verify>
  <done>Repositories expose candidate/publish persistence helpers and cache utilities are available for later plans.</done>
</task>

</tasks>

<verification>
- New ORM models load without SQLAlchemy errors in module import.
- Repository methods are defined and callable from app.repositories.
- Redis cache helper imports do not raise on missing env (uses fallback).
</verification>

<success_criteria>
- Candidate/publish tables are defined in code and wired for shop schema creation via Base.metadata.
- Cache helper module provides key building and get/set/delete utilities for candidate lists.
</success_criteria>

<output>
After completion, create `.planning/phases/03-reliable-batch-publishing-and-candidate-store/03-01-SUMMARY.md`
</output>
